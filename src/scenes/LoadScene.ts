class LoadScene extends Phaser.Scene {
  constructor() {
    super({
      key: 'LoadScene'
    });
  }

  preload() {

  }

  create() {
  }

  update(time: number, delta:number) {
  }
}

export default LoadScene;